"use strict";

var React = require('react');

var Map = React.createClass({
    render: function() {
        return (
            <h1>Hello, Everyone! The map will go here</h1>
        );
    }
});

module.exports = Map;