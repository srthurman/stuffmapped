"use strict";

var React = require('react');

var Footer = React.createClass({
    render: function() {
        return (
            <div>
                <footer className="container">
                    FOOTER
                </footer>
            </div>
        );
    }
});

module.exports = Footer;